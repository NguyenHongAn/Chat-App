const defaultState = {
    message: "",
    thread: [],
    currentThread: {}
}

const chatReducer = (state = defaultState, action) =>{
    switch (action.type) {
       
        default:
            return state;
    }
}

export default chatReducer;